<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bususerapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusUserApp">
        <activity android:name=".Schedule.DisplayBusStopNamesActivity"></activity>
        <activity android:name=".Schedule.DisplayScheduleActivity" />
        <activity android:name=".Geofence.GeofenceActivity" />
        <activity android:name=".Schedule.ScheduleSDActivity" />
        <activity android:name=".Schedule.ScheduleBusNoActivity" />
        <activity android:name=".TicketBooking.TicketActivity" />
        <activity android:name=".TicketBooking.RazorPayActivity" />
        <activity android:name=".TicketBooking.TicketBookingMainActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBIKb22QRLoJy9-3UWY4EQsf_lq-XTkpCU" />

        <activity
            android:name=".LiveTrack.LiveMapsActivity"
            android:label="@string/title_activity_live_maps" />
        <activity android:name=".LiveTrack.DisplayBusActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.LostFoundMainActivity" />
        <activity android:name=".Activities.PostActivity" />
        <activity android:name=".Activities.PostViewActivity" />
        <activity android:name=".Activities.ProfileViewActivity" />
        <activity android:name=".Activities.MessageActivity" />
        <activity android:name=".Activities.MessageViewActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>